.sconsign.dblite
config.log
.sconf_temp
*.o
*.os
*.so
*.cfgc
*.pyc
*_wrap.cc
*Lib.py

# Built by sconsUtils
version.py
bin/

# Pytest
tests/.tests
pytest_session.txt
.cache/
.pytest_cache
.coverage

# PyTorch Lightning training artifacts (emitted by retrain / NBEATSx)
lightning_logs/

# Trained model bundles -- binary artifacts, not source (see review comment
# on PR for add-nbeatsx-ridge-forecaster). Canonical shared USDF/S3DF path
# TBD; point WEATHERNBEATS_BUNDLE at wherever the team lands on -- not here.
models/

# Local notes / editor backups / Claude Code workspace
prompt.txt
prompt.txt~
.prompt.txt.un~
*~
*.un~
.claude/
CLAUDE.md
